## 과제 목표
이번 주차 과제를 통해 무엇을 배우고자 했는지 간단히 적어요.
- LangChain `@tool` 데코레이터로 LLM이 직접 호출하는 도구를 만드는 기본 구조를 익히고자 함
- 앱 DB가 아닌 현재 대화 전용 임시 메모리(`PERSONAL_SCHEDULES`)로 일정 CRUD를 구현하고자 함
- `session_id`로 대화 범위를 분리하고, agent가 prompt를 보고 알맞은 tool을 자동 선택하는 흐름을 이해하고자 함

---

## 과제 위치
- 작업 브랜치 : `gang/week1`  → 본인 통합 브랜치 `gang/final` 로 PR
- 주요 파일 : `student_parts/week01_wake_up_nana.py`

> 제공 코드(`fixed/` 등)는 수정하지 않고, `student_parts/` 의 본인 주차 파일만 구현함.
> 리뷰어(담당 멘토)는 PR 을 열면 자동 지정되므로 직접 추가하지 않음.

---

## 구현한 기능
이번 주차 **기본 미션** 중 구현한 항목에 체크해요.
- [x] `personal_create_schedule` — 입력값으로 일정 dict 생성 후 임시 메모리에 저장, `created_schedule` 반환
- [x] `personal_list_schedules` — 현재 대화 범위 일정을 날짜(`date_from`~`date_to`)로 필터해 `schedules` 반환
- [x] `personal_delete_schedule` — 현재 대화 범위에서 `schedule_id` 일치 일정만 삭제, `deleted` 반환

---

## 도전 기능
**도전 미션**을 시도했다면 체크하고 간단히 적어요.
- [ ] (해당 없음)

---

## AI 활용 내역
AI를 활용해 구현하거나 수정한 내용을 기록해요.

### 반환 JSON 형태 확인
- AI 활용 내용 : tool이 dict보다 문자열(JSON)로 반환하는 게 안정적인지 헷갈려서 한 번 물어봄.
- 직접 구현한 부분 : 세 함수 모두 가이드 주석을 보고 직접 구현했고, dict를 만든 뒤 기존 helper `_json(...)`으로 감싸 반환하도록 정리함.
- 비고 : 로직(생성/조회/삭제, `session_id` 분리, 날짜 필터)은 가이드대로 직접 작성함. AI는 반환 타입 확인용으로만 잠깐 사용함.

---

## 구현하면서 고민한 점
막혔던 부분, 고민한 내용, 해결 방법을 자유롭게 적어요.
- 고민한 점 : ① 같은 `schedule_id`가 다른 대화에도 있을 때 잘못 삭제되는 문제, ② 삭제 시 `PERSONAL_SCHEDULES`를 새 객체로 바꾸면 다른 코드의 참조가 끊기는 문제, ③ tool 반환 타입.
- 해결 방법 : ① 모든 조회/삭제에서 `current_session_scope()`와 같은 `session_id`만 대상으로 처리함, ② 슬라이스 대입(`[:]`)으로 리스트 객체는 유지하고 내용만 교체함, ③ dict를 `_json(...)`으로 감싸 문자열(JSON)로 반환함.

---

## 과제 회고 (KPT)
과제를 마치고 KPT 회고를 적어요.
- **Keep** : 가이드 주석의 반환 키와 책임 범위를 먼저 읽고 그대로 맞춤. 상세 trace로 실제 호출과 결과를 눈으로 확인함.
- **Problem** : 처음엔 `session_id` 분리와 `[:]` 대입의 필요성을 바로 이해하지 못해 헷갈림.
- **Try** : 다음 주차의 DB 저장/조회 도구와 연결해보고, 빈 입력·잘못된 날짜 등 예외 케이스 처리도 추가해볼 예정임.
