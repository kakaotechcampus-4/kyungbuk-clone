# 2주차 작업 및 회고

## 작업 내용

- 자연어 요청을 `StructuredRequestBatch`로 변환하는 Pydantic 스키마를 구현했다.
- 개인 일정, 그룹 일정, 할 일, 알림, 분류 불가 요청의 `kind` 판정 기준을 프롬프트에 명시했다.
- `members`에는 사용자 본인을 제외한 실제 참석자만 포함하도록 기준을 정리했다.
- 완료된 `TODO` 주석을 제거하고 structured output이 하나의 JSON 객체만 반환되도록 출력 규칙을 보완했다.

## 검증

개인 일정, 그룹 일정, 할 일, 알림, 애매한 질문을 실제 LLM에 반복 입력했다. 각 입력이 의도한 `kind`로 동일하게 분류되는 것을 확인했고, Python 문법 검사와 Pydantic 스키마 smoke test도 통과했다.

## 회고

스키마의 `Field` 설명만 구체적으로 작성해도 모델의 출력 품질이 좋아지지만, 일관된 결과를 얻으려면 프롬프트에 분류 기준과 우선순위까지 명확히 제공해야 한다는 점을 배웠다. 또한 베이스 코드 업데이트는 직접 다시 작성하지 않고 최신 변경을 브랜치에 반영해야 하며, 구현이 끝난 `TODO`는 동료에게 혼동을 주지 않도록 제거해야 한다.

다음 과제에서는 구현 전에 최신 공지와 베이스 코드를 먼저 확인하고, 라이브러리 공식 문서와 작은 실행 예제로 동작을 검증한 뒤 본 구현을 진행하겠다.
